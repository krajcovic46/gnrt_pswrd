# Use to update the max value which is used to determine the range from which the true random numbers are picked
def update_max():
    with open("words.txt") as f:
        for indx, wrd in enumerate(f):
            pass
    with open("last_mx_vl.txt", 'w') as f:
        f.write(index)
